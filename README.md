# Nodejs - Typescript (NT)
#### Nodejs typescript server boilerplate
___

## Start

### Production `(compile TS then execute)`
```bash
npm start
```

### Development `(compile TS on the fly + live restart)`
```bash
npm run start:dev
```